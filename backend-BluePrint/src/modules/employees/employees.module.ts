import { Module } from '@nestjs/common';

/**
 * Employees Module — Stage 3 (part of Auth/Users)
 *
 * Will implement:
 * - Employee entity (extends User)
 * - Employee profiles: name, role, PIN, QR badge
 * - Shift management
 * - Employee assignment to tasks
 * - Performance tracking data
 * - EmployeesController: CRUD operations
 * - Integration with Tasks module for workload data
 */
@Module({
  imports: [],
  controllers: [],
  providers: [],
  exports: [],
})
export class EmployeesModule {}
